import { useState } from 'react';
export const defaults = { ram_gb:16, storage_gb:512, processor_score:7, gpu_score:5, screen_size:15.6, rating:4.2, brand:'Unknown' };
const labels = {ram_gb:'RAM (GB)',storage_gb:'Storage (GB)',processor_score:'Processor tier',gpu_score:'GPU tier',screen_size:'Screen (inches)',rating:'Rating',brand:'Brand',budget:'Budget (₹)',min_rating:'Minimum rating'};
export function FeatureForm({ onSubmit, budget=false, submitText='Submit' }) { const [values, setValues] = useState({...defaults,...(budget ? {budget:80000,min_rating:4} : {})}); return <form onSubmit={event => {event.preventDefault();onSubmit(values)}}>{Object.entries(values).map(([key,value]) => <label key={key}>{labels[key]}<input type={key === 'brand' ? 'text' : 'number'} min="0" max={key === 'rating'||key === 'min_rating'?5:undefined} step={['screen_size','rating','min_rating'].includes(key)?'0.1':'1'} required={key !== 'brand'} value={value} onChange={event => setValues({...values,[key]:key === 'brand' ? event.target.value : Number(event.target.value)})}/></label>)}<button>{submitText}</button></form>; }
