import { useState } from 'react'; import { api } from '../api'; import { FeatureForm } from './Form';
export default function Recommendation() { const [items,setItems]=useState([]),[error,setError]=useState(''); const submit=async v=>{setError('');setItems([]);try {setItems(await api.recommend(v))}catch(e){setError(e.message)}};return <article><h2>Recommendations</h2><FeatureForm budget submitText="Find laptops" onSubmit={submit}/>{error&&<output>{error}</output>}{items.length === 0 && !error ? <p className="empty">Submit your requirements to see matching laptops.</p> : items.map(x=><p key={x.id ?? x.title}><b>{x.title}</b><br/>₹{Number(x.price).toLocaleString('en-IN')} · {x.ram_gb} GB RAM · {x.rating} ★</p>)}</article> }
